import { StyleSheet, Text, View } from 'react-native';
import ProfileScreen from '../src/presentation/screen/profile/ProfileScreen';
const profiles = () => {
  return <ProfileScreen />;
};
export default profiles;
const styles = StyleSheet.create({});
